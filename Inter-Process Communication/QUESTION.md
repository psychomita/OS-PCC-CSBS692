# Assignment: 6

```
1) Implement IPC between a parent and a child process where parent will print a message received from the child,
   who will take the message as user input. Use un-nnamed pipe for IPC.

2) Implement IPC between two processes where proceess-1 will take two strings as user input and send them to process-2.
   Process-2 will compare them and return the result ("SAME" or "NOT SAME") back to process-1. Use FIFO for IPC.
```
