# Assignment: 3
```
1) Write a menu driven shell script to do the following:
   i) Show the permissions of a file or directory;
   ii) Show the number of files and directories under current directory separately;
   iii) Show the last modification date and time of a file in current directory;
   iv) Exit from menu.

2) Write a menu driven shell script with three options: 
   i) Accept your personal details along with a password and store them in a data file (e.g. personal.dat);
   ii) Display the personal details stored in the data file after successfully matching the password stored in the file;
   iii) Exit from menu.
